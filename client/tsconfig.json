{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "jsx": "react-jsx", // For React 17+ (using the new JSX transform)
    // If using React 16 or below, use: "react"
    "esModuleInterop": true,
    "module": "ESNext", // Or "CommonJS" depending on your setup
    "target": "ES6", // Or higher (e.g., "ESNext")
    "moduleResolution": "node"
  }
}
